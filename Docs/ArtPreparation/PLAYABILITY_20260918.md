# 2026-09-18 플레이 진행·마탑·메뉴 개선

## 확인된 원인과 변경

- 실제 기기 저장은 장비 300개, 개별 보관 76개였다. 기존 코드는 보관 75개를 넘으면 다음 전투를 `ResultPending`으로 정지했다. 적은 사라지고 던전의 `Running` 입장 조건도 충족하지 못했다. 보상은 개별 보관 100개 이후 기존 수량형 저장 구조에 합산하며, 보관함 크기가 스테이지 전환을 막던 코드를 제거했다. 저장된 장비·강화·잠금·장착 정보와 안정적인 코드는 유지한다.
- 장비 뽑기의 가방 공간 제한을 제거했다. 지급은 같은 영구 보관 경로를 사용하며, 실제 재화 부족·처리 중·데이터 준비 오류를 구별한다.
- 스킬 중복은 1회당 파편 30개다. 120개 표시는 4회 중복의 합계이며, 결과 창에 중복 횟수와 파편 수를 별도 줄로 표시한다. 뽑기 확률과 지급량은 변경하지 않았다.
- 왕국군·가방 장비 목록은 보이는 카드만 생성하고 스크롤 시 재사용한다. 내부 영문 저장 키를 바꾸지 않고 화면용 장비 이름을 추가했다.
- 상세 체력의 int 변환을 제거하고 실제 long HP를 표시한다. 계산식은 실제 `PlayerStatus`의 장비·오라·계정·환생·골드 강화 값을 읽으며, 바뀐 계산식만 다시 그린다. 강화 예상치는 실제 무기 성장 함수와 같은 값을 쓴다. 잠금·강화된 장비는 재료 보유량에서 제외한다.
- 전직 카드 높이는 340 → 208, 연결선·초상화·여백을 함께 축소했다. 종합 체력바는 직사각형이다. 가이드·가방은 안전 영역 안의 중앙 팝업이다. 스테이지 배지는 파티 HUD 위, 가이드는 상단 메뉴 바로 아래에 정렬한다. 가이드 보상 수령 시 보상 토스트를 표시한다.
- 자동 시전 OFF에서 장착된 마탑 슬롯만 탑 위로 굴러 올라온다. 짧은 화면에서는 가이드 아래의 남은 높이를 계산해 여러 열로 펼친다. 탭은 자동 조준, 드래그는 지점 지정이다. 원형 범위는 실제 스킬 반경을 투영하며 unscaled-time으로 회전한다. 천벌도 원형 착탄 범위다. 얼음 송곳·유성우는 드래그 시 시전·쿨다운을 소비하지 않는다. UI 위 드롭·전투 전환·포커스 상실은 취소한다.
- 맹독 늪의 이름·설명과 아이콘을 수정했다. 암석 봉인 아이콘은 실제 바위 기둥, 회복의 성역은 실제 빛나는 십자가와 지면 룬을 재사용했다. 성역은 지정 범위 안에서 체력 비율이 가장 낮은 살아 있는 왕국군을 회복한다.

## 검증 증거

Unity 6000.3.21f1, Android 실제 기기 1대(SM-N986N). 물리 패널은 1440×3088이며 작업 시작 시 표시 설정은 1080×2316, density 450이었다. 화면 비율 변경 검사는 같은 기기의 표시 크기 변경이며 여러 실제 기기를 의미하지 않는다.

- 사용자 앱의 내부 설정과 외부 저장은 변경 전 `Recordings/PlayabilityRevision/Original`에 백업했다. 경제·전투 파괴 검사는 격리된 QA 프로필에서 수행했다.
- 에디터 저장·재화 검사 101개: 2,400개 보상의 수량형 보관, 직렬화 재로딩, 64비트 체력, 실제 스탯과 표시 계산식, 동일 수치 갱신 억제를 검증했다. 14,943개 직렬화 참조에서 누락 0개, 스테이지 43개와 장비 18종도 검사했다. `Recordings/BalanceRevision/Editor/validation.json`.
- Unity 실제 로그인 후 전투: 기본 10종·개화 10종의 발동, 양의 피해/회복, 시전 종료를 확인했다. `Editor/Live/results.json`은 passed이며 20종 캡처를 보존했다.
- Android 기본 10종·개화 10종: 전부 발동·효과·종료 검사 통과. `Diagnostic1/all-spells-report.json`과 시전별 원시 상태·캡처.
- Android 마탑 경제 검사 186개: 200회 뽑기의 10개 단위 결과, 중복 30파편, 스킬별 누계와 재화 차감, 각성·개화 저장·실패 시 원자성을 대조했다. `Diagnostic1/transactions.json`.
- Android 보관 500개 상태에서 뽑기 지급, 던전 입장, 메인 복귀와 적 재출현 통과. `Diagnostic1/core-report.json`.
- 실제 Android 드래그: 천벌 원형 범위와 지점 시전, 얼음 송곳 드래그 무효·탭 시전 통과. `Diagnostic1/manual-report.json` 및 드래그 캡처.

- 일반 1–3장, 보스 1–3장, 골드·루비 던전 각 1–5단계의 16개 전투 경로를 통과했다. 실제 입장권 소비·메인 복귀, 근접 유효 위치, 공격 타이밍, 원거리 후퇴 종료를 확인했다. 부적절한 근접 타격 0개, 최대 후퇴 약 0.715초. `Diagnostic1/matrix.json`.
- 군중 제어 중첩·만료, 풀링 재사용, 천벌 착탄 반경 검사 9개 통과. 일반·저사양 모드 각각 90초 중첩 시전과 앱 복귀 중 기록된 런타임 오류 0개. `Diagnostic1/layered-controls.json`, `Diagnostic1/stress.json`. 두 모드 모두 약 60fps였으나 진행 스테이지·시전 시점이 완전히 같지 않아 저사양 절감률로 해석하지 않는다.
- Android 강화 전후 실제 공격력 179 → 184와 상세 UI 184 일치. 왕국군 장비 카드 8개, 가방 15개 생성, 스크롤 재바인딩과 뒤로가기 통과. 스킬 10연 결과의 중복/파편 문구 잘림 0개. `Diagnostic3/menus-report.json`.
- 탑 실제 장압 ON/OFF, 상단 UI 드롭 거부, 화면 밖 좌표 거부, 드래그 중 홈 화면 이동 후 쿨다운 없이 취소를 확인했다. 범위 미리보기 크기 432×432로 원형을 유지했다. `Diagnostic3/aim-guards-report.json`.
- 720×1600 및 900×1200에서 중앙 팝업·닫기·뒤로가기·스킬 상세를 검증했다. 스크린샷에서 별도로 발견한 태블릿 수동 슬롯/가이드 겹침은 가용 높이에 따른 다열 배치로 수정했다. 초기 기하 검사에서 화면 안에 있다는 조건만 검사했던 한계도 회귀 스크립트에 보완했다.

장비 메뉴 열기 전후를 Android SurfaceFlinger의 실제 표시 간격으로 비교했다. 원래 사용자 저장과 300개 QA 장비의 동일 메뉴를 각 6초 구간으로 측정했으며, 저장 내용·배경 전투는 완전히 동일하지 않다.

| 측정 | 최대 프레임 간격 | 50ms 초과 | 샘플 |
|---|---:|---:|---:|
| 수정 전 장비 메뉴 | 972.79ms | 2 | 302 |
| 수정 후 장비 메뉴 | 41.63ms | 0 | 459 |
| 수정 후 가방 | 75.02ms | 1 | 453 |
| 최종 b25 · 원래 계정 장비 메뉴 | 49.91ms | 0 | 483 |
| 최종 b25 · 원래 계정 가방 | 74.93ms | 1 | 480 |

원시 기록은 `Original/equipment-open-performance.json`과 `Diagnostic3/menu-performance.json`, 메뉴별 `presentation-raw.json`에 있다. 평균 전투 fps와 메뉴를 여는 순간의 지연을 구별한다. 검증 도우미는 `AI/qa/mage/playability_checks.py`, `AI/qa/combat/polish_checks.py`다.

## 최종 앱과 실제 계정

최종 설치본은 **0.11.0 (b25), 직접 플레이**, `com.isolatedyouth.idlekingdomrpg.lobbyqa`다. 고정된 Unity 버전에서 ARM64 IL2CPP 빌드 성공, 오류 0개다. 기존 경고 22개는 빌드 로그에 남겼다. `LOBBY_DEVICE_QA`를 포함하지 않아 계정 강제 지정·진단 명령 처리기가 없다. `FinalResponsive/build.json`, `build.txt`와 APK를 보존했다.

일반 타이틀의 로그인 버튼 → 게스트 로그인 → 방치 보상 확인 → 실제 계정의 전투·마탑·장비·가방·던전 메뉴에 진입했다. 원래 계정은 장비 300개, 개별 보관이 75개를 넘은 상태에서 적이 계속 생성되고 처치되며 보스 3으로도 이동했다. 보존 검사 시 처치 수는 31,068 → 31,428, 개별 보관은 76 → 86이었다. 기존 장비·강화·잠금·전직·스킬 보유 기록을 보존했고 고대주화를 소모하지 않았다. `FinalResponsive/user-preservation.json`의 해당 비교는 모두 true다. 최종 앱 프로세스 로그에서 런타임 오류 0개, 기기 내부의 진단 명령 파일 0개를 확인했다.

진단 후 기기 내부·외부 저장을 `PostQA`에 한 번 더 백업했다. 진단으로 생성한 파일 514개를 제거하고 기존 QA 저장 2개와 원래 설정을 되돌렸다. 실제 사용자 저장은 덮어쓰지 않았으며 정상 로그인에 따른 방치 보상과 진행을 유지했다. 이 프로젝트 앱은 기기에 하나만 남겼다.

최종 b25에서도 실제 보유 스킬 5개를 UI로 임시 장착해 900×1200에서 다시 검사했다. `FinalResponsive/final-tablet-fixed-tray.png`에서 3열·2행 슬롯과 가이드가 겹치지 않고 모두 노출됨을 확인했다. 원래 1080×2316 화면에서는 세로 배열이다. 검사 후 원래 화면 크기·밀도와 빈 슬롯 구성을 되돌렸다.

## 아트와 카탈로그

`AI/comfyui/mage-skills/20260918-playability`에 Comfy Cloud 실제 카탈로그·변환 노드 스키마, 원본 6개, 결정론적 조합 스크립트, 출력 6개, 전체 설정과 비교 manifest를 보존했다. 기존 게임 VFX를 정확히 재사용하는 공정이어서 모델 생성 워크플로는 제출하지 않았다. 따라서 API 실행 JSON·UI 그래프·작업 ID는 해당 없음이며 이를 manifest에 명시했다.

아이콘은 48×48, 이진 알파, 투명색 포함 4–8색이다. 기본/개화 실루엣은 같다. 암석 봉인 v1은 실제 기기에서 보인 바위 덩어리보다 기둥이 길고 분리돼 보여 기각했고, v2에서 세 레이어의 겹침·비율·실제 프리팹 색을 반영했다. 두 결과와 비교·채택 이유를 보존했다. 최종 스프라이트는 Atlas_UIPixel, VFX는 기존 Atlas_MageVFX 생성 경로로 포함한다.

아이콘 크기는 늘리지 않았다. 수동 UI는 최대 5개 슬롯이며 조준 마법진은 텍스처 없이 작은 UGUI 메시를 그린다. 성역에는 기존 시트의 십자가 렌더러 1개를 추가했다. 같은 VFX 아틀라스를 사용하고 기존 종료·풀링 경로로 정리한다.

유료 생성 호출 0회. 동일 1개월 구간의 실제 사용 보고 전후가 모두 $38.154966로 같아 이번 작업 추가 생성 지출은 **$0**이다. 이 금액은 과거 계정 합계와 구별한다. `usage-before.json`, `usage-after-matching.json`.

기존 `Skill_Catalog.xlsx`에서 이름·설명 3개 셀을 수정하고 종속 Bloom/Acquisition 이름을 재계산했다. 재로딩 후 8개 탭, 스타일·표·고정 창, 관련 없는 값과 수식을 보존했음을 확인했다.

## 범위

현재 스테이지 카탈로그는 일반 3장까지다. 기존 사용자는 3-11을 이미 클리어한 상태였으며, 이후 3-10 반복 사냥은 정상 동작이다. 이번 수정은 적이 없는 정지 상태를 없애며 새 4장을 추가하지 않는다.

실기기 한 대의 검사로 모든 Android 하드웨어·장기간 플레이를 보증할 수는 없다. 검증하지 않은 별도 기종과 실제 서버 저장 권한은 이번 확인 범위에 포함하지 않는다.
