{
  "content": [
    {
      "type": "text",
      "text": "Workflow submitted. prompt_id: 7c845aeb-db5b-44bc-819b-197e1678c6f9\nNote - This workflow includes paid partner-API node(s): Node #10: GeminiNanoBanana2V2 — running it will incur partner-API charges on the user's account.\nNote - Dynamic-combo sub-field values are applied per the selected option's schema — fetch the output (get_output) and compare the width/height it reports (or measure the downloaded file) against the requested size before reporting it as honoured.\nJob queued — poll wait_for_job (or get_job_status) with this prompt_id, then get_output. Conversion/slot warnings above are non-fatal; the job will run as submitted. Do not cancel or resubmit to address a warning. If this is one of several independent generations from a single request, submit the rest in ONE submit_batch call instead of one at a time — decide that yourself; the user need not ask."
    }
  ],
  "structuredContent": {
    "result": {
      "prompt_id": "7c845aeb-db5b-44bc-819b-197e1678c6f9",
      "lifted_from_save": false,
      "lifecycle": "\nJob queued — poll wait_for_job (or get_job_status) with this prompt_id, then get_output. Conversion/slot warnings above are non-fatal; the job will run as submitted. Do not cancel or resubmit to address a warning. If this is one of several independent generations from a single request, submit the rest in ONE submit_batch call instead of one at a time — decide that yourself; the user need not ask.",
      "status": "succeeded_with_warnings",
      "warnings": [
        {
          "code": "paid_api_nodes",
          "detail": "This workflow includes paid partner-API node(s): Node #10: GeminiNanoBanana2V2 — running it will incur partner-API charges on the user's account."
        },
        {
          "code": "dynamic_combo_subfield_advisory",
          "detail": "Dynamic-combo sub-field values are applied per the selected option's schema — fetch the output (get_output) and compare the width/height it reports (or measure the downloaded file) against the requested size before reporting it as honoured."
        }
      ]
    }
  }
}